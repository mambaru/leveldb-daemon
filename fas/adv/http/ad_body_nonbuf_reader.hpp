//
// Author: Vladimir Migashko <migashko@faslib.com>, (C) 2007
//
// Copyright: See COPYING file that comes with this distribution
//

#ifndef FAS_ADV_HTTP_BODY_NONBUF_READER_HPP
#define FAS_ADV_HTTP_BODY_NONBUF_READER_HPP

#include <fas/misc/http.hpp>
#include <fas/adv/http/tags.hpp>

namespace fas{ namespace adv{ namespace http{

namespace amh = ::fas::misc::http;

/*
class ad_body_nonbuf_reader
{
public:
  template<typename T>
  void activate(T& t)
  {
  }

  template<typename T>
  void operator ()(T& t, const char *d, size_t s)
  {
    if (s == 0) return;

  }
};
*/
}}}


#endif
