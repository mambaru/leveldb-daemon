//
// Author: Vladimir Migashko <migashko@faslib.com>, (C) 2007
//
// Copyright: See COPYING file that comes with this distribution
//

#ifndef FAS_UTIL_MEMCACHED_TAGS_HPP
#define FAS_UTIL_MEMCACHED_TAGS_HPP

namespace fas{ namespace util { namespace memcached{

struct _parser_{};
struct _writer_{};

/*struct _add_{};
struct _set_{};
struct _replace_{};*/

struct _storage_{};
struct _get_{};

}}}

#endif
